version: "3.7"

volumes:
  jenkins_home: {}

services:
  jenkins:
    image: jenkins/jenkins:${JENKINS_VERSION}
    ports:
    - 127.0.0.1:8080:8080
    - 127.0.0.1:50000:50000
    volumes:
    - jenkins_home:/var/jenkins_home
